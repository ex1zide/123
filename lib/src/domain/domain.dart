/// Domain Layer — LegalHelp KZ
///
/// This layer contains:
/// - **Entities**: Core business objects (e.g., `LegalCase`, `LawyerProfile`).
/// - **Repositories (interfaces)**: Abstract contracts for data access.
/// - **Value Objects**: Immutable domain primitives.
///
/// The domain layer has **zero** dependencies on Flutter, Firebase, or any
/// infrastructure package. It is pure Dart.
///
/// TODO: Populate with business entities in Phase 2.
library;
